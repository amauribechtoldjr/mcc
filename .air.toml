root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main.exe ./cmd/api"
  bin = "./tmp/main.exe"

  include_ext = ["go", "tmpl", "sql"]
  exclude_dir = ["tmp", "internal/db/migrations"]

  delay = 300
  stop_on_error = true

[misc]
  clean_on_exit = true