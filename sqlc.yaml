version: "2"
sql:
  - engine: "postgresql"
    queries:
      - "./internal/adapter/repository/postgres/sqlc/queries.sql"
    schema: "./internal/adapter/repository/postgres/migrations"
    gen:
      go:
        package: "repo"
        out: "./internal/adapter/repository/postgres/sqlc"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
        emit_pointers_for_null_types: true
        overrides:
          - db_type: "uuid"
            go_type:
              import: "github.com/google/uuid"
              type: "UUID"
          - db_type: "pg_catalog.numeric"
            go_type: "github.com/shopspring/decimal.Decimal"
          - db_type: "pg_catalog.numeric"
            nullable: true
            go_type: "github.com/shopspring/decimal.NullDecimal"
